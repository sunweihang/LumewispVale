import { _decorator, Component } from 'cc';

const { ccclass } = _decorator;

/**
 * Retired: opening meteor inspect loop. Town gate is guided by QuestSystem 1009.
 * Kept as an empty component so old scene bindings do not throw.
 */
@ccclass('FirstQuest')
export class FirstQuest extends Component {}

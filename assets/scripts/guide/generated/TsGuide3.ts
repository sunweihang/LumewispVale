/*
 * AUTO-GENERATED by tools/guide/seed_goto_guides.mjs (guide 3).
 * Prefer re-export from guide-editor after graph edits.
 */
import { AbsGuide } from '../AbsGuide';

export class TsGuide3 extends AbsGuide {
    public readonly guideId = 3;

    protected onResolve(): void {
        void this.step_0();
    }

    private step_0(): void {
        if (this.tryBagToHotbar("seeds", {
            ensureHoe: false,
            openTip: "露穗：点开背包，把种子拿出来",
        })) {
            // aimed
        } else {
            void this.step_1();

        }
    }

    private step_1(): void {
        if (this.trySelectTool("seeds")) {
            // aimed
        } else {
            void this.step_2();

        }
    }

    private step_2(): void {
        if (this.tryWorldPlot("tilled", "露穗：点翻好的地播种")) {
            // aimed
        } else {
            // end

        }
    }
}

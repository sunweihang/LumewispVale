/*
 * AUTO-GENERATED by tools/guide/seed_goto_guides.mjs (guide 26).
 * Prefer re-export from guide-editor after graph edits.
 */
import { AbsGuide } from '../AbsGuide';

export class TsGuide26 extends AbsGuide {
    public readonly guideId = 26;

    protected onResolve(): void {
        void this.step_0();
    }

    private step_0(): void {
        if (this.tryBagToHotbar("hoe", {
            ensureHoe: true,
            openTip: "露穗：点开背包，把锄头拿出来",
        })) {
            // aimed
        } else {
            void this.step_1();

        }
    }

    private step_1(): void {
        if (this.trySelectTool("hoe")) {
            // aimed
        } else {
            void this.step_2();

        }
    }

    private step_2(): void {
        if (this.tryWorldDecor("rock", "露穗：点石头挖石料")) {
            // aimed
        } else {
            // end

        }
    }
}

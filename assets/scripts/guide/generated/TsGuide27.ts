/*
 * AUTO-GENERATED by tools/guide/seed_goto_guides.mjs (guide 27).
 * Prefer re-export from guide-editor after graph edits.
 */
import { AbsGuide } from '../AbsGuide';

export class TsGuide27 extends AbsGuide {
    public readonly guideId = 27;

    protected onResolve(): void {
        void this.step_0();
    }

    private step_0(): void {
        if (this.tryBagToHotbar("axe", {
            ensureHoe: false,
            openTip: "露穗：点开背包，把斧头拿出来",
        })) {
            // aimed
        } else {
            void this.step_1();

        }
    }

    private step_1(): void {
        if (this.trySelectTool("axe")) {
            // aimed
        } else {
            void this.step_2();

        }
    }

    private step_2(): void {
        if (this.tryWorldDecor("tree", "露穗：点树砍几下，攒木料")) {
            // aimed
        } else {
            // end

        }
    }
}

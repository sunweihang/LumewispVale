/*
 * AUTO-GENERATED by tools/guide/seed_goto_guides.mjs (guide 4).
 * Prefer re-export from guide-editor after graph edits.
 */
import { AbsGuide } from '../AbsGuide';

export class TsGuide4 extends AbsGuide {
    public readonly guideId = 4;

    protected onResolve(): void {
        void this.step_0();
    }

    private step_0(): void {
        if (this.tryBagToHotbar("can", {
            ensureHoe: false,
            openTip: "露穗：点开背包，把水壶拿出来",
        })) {
            // aimed
        } else {
            void this.step_1();

        }
    }

    private step_1(): void {
        if (this.trySelectTool("can")) {
            // aimed
        } else {
            void this.step_2();

        }
    }

    private step_2(): void {
        if (this.tryWorldPlot("water", "露穗：给作物浇点水")) {
            // aimed
        } else {
            // end

        }
    }
}

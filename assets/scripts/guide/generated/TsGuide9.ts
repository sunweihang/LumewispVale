/*
 * AUTO-GENERATED by tools/guide/seed_goto_guides.mjs (guide 9).
 * Prefer re-export from guide-editor after graph edits.
 */
import { AbsGuide } from '../AbsGuide';

export class TsGuide9 extends AbsGuide {
    public readonly guideId = 9;

    protected onResolve(): void {
        void this.step_0();
    }

    private step_0(): void {
        if (this.trySelectTool("hand")) {
            // aimed
        } else {
            void this.step_1();

        }
    }

    private step_1(): void {
        if (this.tryWorldDecor("grass", "露穗：点这里拔掉杂草")) {
            // aimed
        } else {
            // end

        }
    }
}
